import React from 'react'

const Profile = ({ children }) => {
    return (
        <section>
            <h1>Profile Page</h1>
            {children}
        </section>
    )
}

export default Profile
